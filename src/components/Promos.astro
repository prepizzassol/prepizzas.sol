---
const {
  img = "/images/promocion.webp", // poné la imagen del cliente en /public
  whatsapp = "https://wa.me/5491133544047?text=Hola!%20Vi%20la%20promo%20de%20Prepizzas.SoL%20🍕%20y%20quiero%20hacer%20mi%20pedido!",
} = Astro.props;
---

<section id="promos" class="relative overflow-hidden bg-bg">
  <!-- textura + fades -->
  <div class="absolute inset-0 z-[1] bg-[url('/texture-1.webp')] bg-repeat bg-cover opacity-10"></div>
  <div class="pointer-events-none absolute top-0 left-0 right-0 h-16 z-[2] bg-gradient-to-b from-bg to-transparent"></div>
  <div class="pointer-events-none absolute bottom-0 left-0 right-0 h-20 z-[2] bg-gradient-to-t from-bg to-transparent"></div>

  <div class="container relative z-[3] py-14 md:py-20 grid grid-cols-1 md:grid-cols-2 gap-10 items-center">
    <!-- Texto (izquierda en desktop / primero en mobile) -->
    <div class="order-1 md:order-none flex flex-col gap-5">
      <span class="inline-flex w-max items-center gap-2 rounded-full bg-accent/15 text-accent px-3 py-1 text-sm font-semibold">
        Llevá más, pagá menos
      </span>

      <h2 class="font-serif text-primary text-3xl md:text-4xl leading-tight">
        Promociones que <span class="font-hand text-accent">rinden</span>
      </h2>

      <p class="text-muted max-w-prose">
        Prepizzas caseras listas para el horno o la parrilla. ¡Ideales para freezer y para resolver en el día!
      </p>

      <!-- Bloque de precios prolijo -->
      <div class="rounded-2xl bg-white/90 backdrop-blur shadow-2xl ring-1 ring-black/5 p-5 md:p-6">
        <ul class="space-y-4">
          <li class="flex items-center justify-between gap-4">
            <div class="text-sm md:text-base">Llevando <strong>5 Prepizzas</strong> o más (c/u)</div>
            <div class="shrink-0 inline-flex items-center rounded-full bg-primary text-white px-3 py-1 font-semibold"> $1800 </div>
          </li>
          <li class="flex items-center justify-between gap-4">
            <div class="text-sm md:text-base">Llevando <strong>6 Pizzas Individuales</strong> o más</div>
            <div class="shrink-0 inline-flex items-center rounded-full bg-primary text-white px-3 py-1 font-semibold"> $900 </div>
          </li>
          <li class="flex items-center justify-between gap-4">
            <div class="text-sm md:text-base">Llevando <strong>3 docenas de Pizzetas</strong></div>
            <div class="shrink-0 inline-flex items-center rounded-full bg-primary text-white px-3 py-1 font-semibold"> $10.000 </div>
          </li>
        </ul>

        <div class="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-3 text-sm text-muted">
          <div class="inline-flex items-center gap-2"><span class="w-1.5 h-1.5 rounded-full bg-accent"></span> Pedidos en el día</div>
          <div class="inline-flex items-center gap-2"><span class="w-1.5 h-1.5 rounded-full bg-accent"></span> Aptas freezer</div>
        </div>

        <div class="mt-5 flex flex-wrap gap-3">
          <a href={whatsapp} target="_blank" rel="noopener" class="btn">Hacer pedido</a>
          <a href="#contacto" class="btn bg-accent text-white hover:opacity-90">Consultar</a>
        </div>
      </div>
    </div>

    <!-- Imagen (derecha en desktop / segunda en mobile) -->
    <div class="order-2 md:order-none flex justify-center md:justify-end">
      <figure class="relative w-full max-w-xl rounded-2xl overflow-hidden shadow-2xl bg-white">
        <img
          src={img}
          alt="Promoción vigente"
          class="w-full h-auto object-cover transition-transform duration-500 hover:scale-[1.02]"
          loading="lazy"
        />
        <!-- borde y glow sutil -->
        <div class="pointer-events-none absolute inset-0 ring-1 ring-black/10"></div>
      </figure>
    </div>
  </div>
</section>
